# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'MenuTransition' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for MenuTransition
    pod 'SnapKit', '~> 5.0.0'
    pod 'Closures'

  target 'MenuTransitionTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'MenuTransitionUITests' do
    # Pods for testing
  end

end
